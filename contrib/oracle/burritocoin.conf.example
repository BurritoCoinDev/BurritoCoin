# BurritoCoin mainnet node — Oracle A1 (seed / explorer backend).
#
# Install to /home/ubuntu/.burritocoin/burritocoin.conf (chmod 600).
# Generate the rpcauth line first:
#   python3 ~/BurritoCoin/share/rpcauth/rpcauth.py brtorpc
# It prints an rpcauth=... line and the plaintext password. Put the rpcauth
# line here; keep the password for ElectrumX and the explorer .env.

server=1
listen=1
txindex=1

# No keys on this box. The build cannot use --disable-wallet (libmw's wallet
# sources are not gated behind ENABLE_WALLET, so the flag fails to compile),
# so the wallet is compiled in but switched off at runtime instead. This is
# the stronger guarantee anyway: no wallet is loaded and none can be created.
disablewallet=1

# RPC — loopback only. ElectrumX and btc-rpc-explorer are the only clients.
# Never expose 9226; the VCN security list must not open it either.
rpcbind=127.0.0.1
rpcallowip=127.0.0.1
rpcport=9226
# rpcauth=brtorpc:<salt>$<hash>

# P2P
port=9227
maxconnections=40

# Keep serving the chain while nobody is mining. By default a node whose newest
# block is more than 24 hours old treats itself as still syncing, and a node in
# that state ignores getheaders from peers (net_processing.cpp). On a quiet
# chain the seed therefore refused to hand the chain to any new node, and every
# fresh wallet sat at block 0. Ten years here means "a quiet chain is not an
# unsynced one". Safe on the seed, which already holds the whole chain; mainnet's
# nMinimumChainWork is 0, so don't copy this onto a node that hasn't synced yet.
maxtipage=315360000

# No addnode= line is needed: the compiled-in fixed seed and the DNS seed
# (seed.burritoco.in) both resolve to this host, and peer-exchange takes over
# from there. The old Linode mesh peer was removed when that host was retired.
